apiVersion: v1
kind: Secret
metadata:
  name: restic-secrets-manager
  labels:
    app: restic-secrets-manager
type: Opaque
stringData:
  # WARNING: placeholder value — replace it with a strong random key before
  # deploying (e.g. `openssl rand -base64 48`). Anyone holding this key can
  # forge authentication sessions.
  JWT_KEY: CHANGE_ME
